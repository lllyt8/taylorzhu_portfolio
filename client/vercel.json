{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "buildCommand": "npm install vite@6.3.2 --save-dev && npm install && node ./node_modules/vite/bin/vite.js build",
  "outputDirectory": "dist"
}
