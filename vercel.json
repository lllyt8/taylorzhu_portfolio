{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "buildCommand": "cp vercel-package.json package.json && npm install && npm run build",
  "outputDirectory": "dist",
  "installCommand": "echo 'Skipping global install'",
  "framework": "vite"
}
